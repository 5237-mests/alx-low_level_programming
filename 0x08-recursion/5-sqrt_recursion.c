#include "main.h"

/**
 *-sqrt_recursion - gives natural square root
 *@n: input num
 *Return: square root of given num
 */
int _sqrt_recursion(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	else
		return ();
}
